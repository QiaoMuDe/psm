# Checklist

- [x] variables.css 中存在 `--font-size-offset: 0px` 变量定义
- [x] components.css 中所有 font-size 使用 calc() 格式
- [x] layout.css 中所有 font-size 使用 calc() 格式
- [x] variables.css 中 font-size 使用 calc() 格式
- [x] 设置页显示字体大小选择器（5 个预设档位 + 自定义选项）
- [x] 选择字体大小后界面实时预览变化
- [x] 选择"自定义"时显示输入框
- [x] 自定义输入验证：限制在 -5 到 +5 范围内
- [x] 保存设置后 font_size_offset 存入数据库
- [x] 应用启动时自动加载并应用字体偏移量
- [x] 数据库 settings 表存在 font_size_offset 默认值
- [x] 切换字体大小后重启应用设置仍然生效
