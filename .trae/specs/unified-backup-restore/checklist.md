# 验证清单

## 备份功能

- [x] 备份按钮点击后弹出文件保存对话框
- [x] 生成的 ZIP 文件包含 `data.json` 和 `skills/` 目录
- [x] `data.json` 包含 settings、prompts、skills 三张表的所有数据
- [x] `skills/` 目录包含所有 Skill 的实际文件
- [x] 无 Prompt 和 Skill 时仍能正常备份（仅包含 settings）

## 恢复功能

- [x] 恢复按钮点击后弹出确认对话框
- [x] 确认后弹出文件选择对话框
- [x] 选择有效备份包后成功恢复数据
- [x] 恢复后 settings 表数据正确更新
- [x] 恢复后 prompts 表数据正确插入（同名跳过）
- [x] 恢复后 skills 表数据正确插入（同名跳过）
- [x] 恢复后 Skill 文件正确复制到存储目录
- [x] 恢复完成后显示结果统计（恢复了多少条 Prompt、多少个 Skill）

## 错误处理

- [x] 选择不包含 `data.json` 的 ZIP 文件时显示「备份文件格式无效」
- [x] 选择损坏的 ZIP 文件时显示「备份文件损坏，无法恢复」

## 构建验证

- [x] `wails build` 构建成功，无报错
- [x] 设置页面「数据管理」区域正确显示
- [x] 备份和恢复端到端流程验证通过
