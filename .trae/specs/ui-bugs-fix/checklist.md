# Checklist

- [x] 技能工具栏末尾无多余竖杠分隔符
- [x] 提示词工具栏布局不受影响，保持原样
- [x] `SanitizeFileName` 函数正确替换 `\ / : * ? " < > |` 为 `_`
- [x] 名字无特殊字符时 `SanitizeFileName` 返回原值
- [x] 导入含冒号名字的技能包后目录名正确（如 `go-kit_core`）
- [x] 导入含斜杠名字的技能包后目录名正确（如 `go-kit_core`）
- [x] PSM 格式批量导入时同样处理特殊字符
- [x] 提示词模块搜索后切换到其他模块再切回，搜索框为空且列表显示全部
- [x] 技能模块搜索后切换到其他模块再切回，搜索框为空且列表显示全部
- [x] 提示词模块分类筛选切换页面后同步重置为"所有分类"
- [x] 标签筛选切换页面后同步重置
